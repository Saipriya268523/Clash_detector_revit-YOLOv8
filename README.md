# Clash_detector_revit-YOLOv8
A small end-to-end pipeline that detects structural-MEP clashes (e.g. a duct passing through a column, beam, or slab) from 3D BIM screenshots, using a custom Revit model as the data source and YOLOv8 for detection.
